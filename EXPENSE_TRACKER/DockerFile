FROM python:3.9

WORKDIR /app
COPY . /app

# Install tkinter and system dependencies
RUN apt-get update && \
    apt-get install -y python3-tk && \
    pip install -r requirements.txt && \
    rm -rf /var/lib/apt/lists/*

# Run the GUI app only if DISPLAY is available
CMD ["python", "app.py"]
